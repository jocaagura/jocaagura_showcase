name: jocaagura_showcase
description: Easily showcase and visualize the components, colors, pages, and widgets of your Flutter theme.
version: 0.0.2
homepage:

environment:
  sdk: '>=3.1.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
flutter:
